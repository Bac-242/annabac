# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
# environment
.env
.env.production
# Cloudflare local secrets / build
.dev.vars
.wrangler/
# Exports de l'ancienne base D1 de modération : contiennent des données
# personnelles (e-mail de l'admin, empreintes IP). Ne jamais committer.
backup-annabac*.sql
# Claude Code workspace
.claude/
# macOS
.DS_Store
# Dossier source des recueils (à la racine uniquement, pas src/content/sujets)
/sujets/
# Artefacts de compilation LaTeX (les sources .tex de sources/ sont versionnées)
*.aux
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc
