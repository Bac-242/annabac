# Configuration Cloudflare Pages — site entièrement statique (Astro).
# Le site est servi depuis dist/ ; il n'y a plus d'API ni de bindings.
# Les contributions arrivent par e-mail (voir /contribuer).
name = "annabac"
compatibility_date = "2025-01-01"
pages_build_output_dir = "dist"
